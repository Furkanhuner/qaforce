{
  "name": "@qaforge/visual",
  "version": "0.1.0",
  "description": "Visual regression agent — pixelmatch diff + LLM vision classification for qaforge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean",
    "test": "vitest run",
    "dev": "tsup src/index.ts --format cjs --dts --watch"
  },
  "dependencies": {
    "@qaforge/core": "workspace:*",
    "@qaforge/providers": "workspace:*",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0",
    "playwright": "^1.44.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pixelmatch": "^5.2.6",
    "@types/pngjs": "^6.0.4",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "files": ["dist"],
  "publishConfig": { "access": "public" }
}
