{
  "name": "@qaforge/core",
  "version": "0.1.0",
  "description": "Core config, types, and session management for qaforge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean",
    "test": "vitest run",
    "dev": "tsup src/index.ts --format cjs --dts --watch"
  },
  "dependencies": {
    "jiti": "^1.21.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.14.0",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.44.0"
  },
  "files": ["dist", "README.md"],
  "publishConfig": { "access": "public" }
}
